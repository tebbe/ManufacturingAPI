CREATE TABLE [dbo].[DiscountType] (
    [Id]               INT           NOT NULL,
    [DiscountTypeName] VARCHAR (300) NOT NULL,
    CONSTRAINT [DiscountType_Primary_Key] PRIMARY KEY CLUSTERED ([Id] ASC)
);

