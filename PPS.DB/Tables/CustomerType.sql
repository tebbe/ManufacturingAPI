CREATE TABLE [dbo].[CustomerType] (
    [Id]               INT          NOT NULL,
    [CustomerTypeName] VARCHAR (50) NOT NULL,
    CONSTRAINT [CustomerType_Primary_Key] PRIMARY KEY CLUSTERED ([Id] ASC)
);

